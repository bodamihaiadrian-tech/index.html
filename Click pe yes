<!DOCTYPE html>
<html lang="ro">
<head>
  <meta charset="UTF-8">
  <title>Cadoul 🎁</title>
  <!-- 1. Include the EmailJS SDK -->
  <script type="text/javascript" src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>

  <!-- 2. Initialize EmailJS with your Public Key -->
  <script type="text/javascript">
    // 👇 YOUR PUBLIC KEY
    (function(){
      emailjs.init("-loWGZVR-RMsaciDO");
    })();
  </script>

  <style>
    body { height: 100vh; display: flex; align-items: center; justify-content: center; font-family: Arial, sans-serif; background: #000; color: white; overflow: hidden; }
    .container { text-align: center; z-index: 10; }
    button { padding: 15px 30px; font-size: 18px; cursor: pointer; position: absolute; }
  </style>
</head>
<body>

<div class="container">
  <h2>Dacă dorești să vezi cadoul, apasă pe YES 🎁</h2>
  <button id="yesBtn">YES</button>
</div>

<script>
  const btn = document.getElementById("yesBtn");

  // --- RE-ADDED: Logic for slowing down the button ---
  let hoverCount = 0;
  const slowDownThreshold = 50; // The button will slow down after this many tries

  btn.addEventListener("mouseover", () => {
    // Increment the counter on each hover
    hoverCount++;

    // Check if the counter has reached the threshold
    if (hoverCount >= slowDownThreshold) {
      // Add a smooth transition effect to the button's movement
      btn.style.transition = "top 0.5s, left 0.5s";
    }

    // Move the button to a random position
    const x = Math.random() * (window.innerWidth - btn.offsetWidth);
    const y = Math.random() * (window.innerHeight - btn.offsetHeight);
    btn.style.left = x + "px";
    btn.style.top = y + "px";
  });


  // --- SIMPLIFIED CLICK ACTION ---
  btn.addEventListener("click", () => {
    // Hide the button
    btn.style.display = 'none';

    // --- Define your settings ---
    const redirectUrl = "https://www.youtube.com/watch?v=dQw4w9WgXcQ";
    const emailParams = {
        promo_code: "PROMO-ULTIMATE-GIFT",
        to_email: "recipient-email@example.com"
    };
    
    // --- This function sends the email in the background ---
    const sendEmailInBackground = () => {
      // 👇 YOUR SERVICE ID and TEMPLATE ID
      emailjs.send('service_r3piudo', 'template_ezoin6k', emailParams)
        .then(res => console.log('Email successfully queued!', res.status))
        .catch(err => console.error('Email sending failed:', err));
    };

    // 1. Send the email
    sendEmailInBackground();

    // 2. Immediately redirect
    window.location.href = redirectUrl;
  });
</script>

</body>
</html>
